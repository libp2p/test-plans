# Python Hole-Punch Interop Test (py-libp2p v0.2.9)

Implements DCUtR hole punching for py-libp2p v0.2.9.

## Local Testing

```bash
cd hole-punch-interop/impl/python/v0.2.9
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
make build