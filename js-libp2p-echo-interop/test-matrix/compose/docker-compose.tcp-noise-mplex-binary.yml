# Docker Compose configuration for test: tcp-noise-mplex-binary
# Generated automatically by test matrix generator

version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: echo-redis-tcp-noise-mplex-binary
    networks:
      - echo-network-tcp-noise-mplex-binary
    command: redis-server --appendonly no --save ""
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  js-echo-server:
    build:
      context: ../images/js-echo-server
      dockerfile: Dockerfile
    container_name: echo-server-tcp-noise-mplex-binary
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - echo-network-tcp-noise-mplex-binary
    environment:
      TRANSPORT: "tcp"
      SECURITY: "noise"
      MUXER: "mplex"
      TEST_SCENARIO: "binary"
      TEST_NAME: "tcp-noise-mplex-binary"
      JS_VERSION: "latest"
      PY_VERSION: "v0.5.0"
      REDIS_ADDR: "redis:6379"
      IS_DIALER: "false"

  py-test-harness:
    build:
      context: ../images/py-test-harness
      dockerfile: Dockerfile
    container_name: echo-client-tcp-noise-mplex-binary
    depends_on:
      redis:
        condition: service_healthy
      js-echo-server:
        condition: service_started
    networks:
      - echo-network-tcp-noise-mplex-binary
    environment:
      TRANSPORT: "tcp"
      SECURITY: "noise"
      MUXER: "mplex"
      TEST_SCENARIO: "binary"
      TEST_NAME: "tcp-noise-mplex-binary"
      JS_VERSION: "latest"
      PY_VERSION: "v0.5.0"
      REDIS_ADDR: "redis:6379"
      IS_DIALER: "true"
    volumes:
      - test-results-tcp-noise-mplex-binary:/app/results

networks:
  echo-network-tcp-noise-mplex-binary:
    driver: bridge

volumes:
  test-results-tcp-noise-mplex-binary:
    driver: local
